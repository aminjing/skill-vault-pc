# 接口通道总览（用户提供 2026-08-15，key 均在 crush/creds/*.key）

## 视觉识别（主：kimi）
- base_url: `https://api.kimi.com/coding/v1`（OpenAI 格式）/ `https://api.kimi.com/coding/`（Anthropic）
- 模型：kimi-k3 优先 → 持续超时 3-5 次 → kimi-k2.7
- key: `creds/kimi.key`
- 已实测：读图/视频审查（本项目 24 张风格图审查均走此链路）

## 交叉验证（gpt）
- base_url: `https://api.codexing.cn/v1`（OpenAI 格式）
- 模型：gpt-5.6-sol
- key: `creds/codexing.key`
- 用途：Agent 互验（Hermes ↔ kimi 之外的第三方裁决）

## 图片生成（codexing 中转）
- base_url: `https://api.codexing.cn/v1`
- **主通道 image-Adobe**：模型 `gpt-image-2-adobe`，key: `creds/adobe.key`
- **次选 bananapro**：模型 `gpt-image-2`，key: `creds/bananapro.key`（仅 Adobe 持续超时/不可用时才选）
- 端点：`/images/generations`（文生图）、`/images/edits`（图生图 multipart）
- 实测：generations/edits 均 200 可用；edits 垫图风格保真偏水彩（待强化 prompt 解决）
